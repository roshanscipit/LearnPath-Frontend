{
  "moduleId": "python",
  "moduleName": "Python Developer",
  "variants": ["Django/Flask", "Data Science", "AI/ML", "Automation"],
  "levels": [
    {
      "level": "Basic",
      "topics": [
        {
          "id": "py-basic-syntax",
          "title": "Python Syntax, Variables & Data Types",
          "summary": "Python's dynamic typing model and the core built-in types used in almost every program.",
          "keyPoints": [
            "Dynamic typing: variables are references to objects, not fixed-type storage",
            "Core types: int, float, str, bool, and Python 3's arbitrary-precision integers",
            "Mutable vs immutable types (list/dict/set are mutable; tuple/str/int are immutable)",
            "Indentation-based blocks instead of braces"
          ],
          "interviewQA": [
            {
              "question": "What does it mean that Python is 'dynamically typed', and what's the trade-off?",
              "keywords": ["dynamic typing", "runtime", "type hints", "duck typing", "flexibility"],
              "modelAnswer": "In Python, a variable is just a name bound to an object, and its type is determined by the object it currently references, checked at runtime rather than compile time. This gives huge flexibility and less boilerplate, but it means type errors surface only when the code actually runs, and IDEs/readers have less static information. Python 3.5+ added optional type hints (PEP 484) to get some of the benefits of static typing via tools like mypy, without changing the runtime behavior.",
              "tip": "Mention type hints and mypy as the modern middle ground.",
              "difficulty": "Easy"
            }
          ]
        },
        {
          "id": "py-basic-control-functions",
          "title": "Control Flow & Functions",
          "summary": "Conditionals, loops, and defining reusable functions with Python's flexible argument system.",
          "keyPoints": [
            "if/elif/else, for (with range/enumerate), while, and Python 3.10+ match statement",
            "Function definitions with default args, *args, **kwargs",
            "First-class functions: passing functions as arguments, returning them",
            "Scope rules: LEGB (Local, Enclosing, Global, Built-in)"
          ],
          "interviewQA": [
            {
              "question": "What's the danger of using a mutable default argument in a Python function?",
              "keywords": ["mutable default argument", "shared state", "list", "gotcha", "none default"],
              "modelAnswer": "Default argument values are evaluated once at function definition time, not on every call. If you use a mutable default like def f(x, lst=[]), that same list object is reused and mutated across every call that doesn't pass its own list, causing unexpected shared state between calls. The standard fix is to default to None and create a new list inside the function body if it's None.",
              "tip": "Give the classic def append_to(x, lst=[]) example to demonstrate the bug.",
              "difficulty": "Medium"
            }
          ]
        },
        {
          "id": "py-basic-collections",
          "title": "Lists, Tuples, Dicts & Sets",
          "summary": "Python's built-in collection types, their performance characteristics, and idiomatic usage.",
          "keyPoints": [
            "List: ordered, mutable, O(1) index access, O(n) search",
            "Tuple: ordered, immutable, hashable if contents are hashable",
            "Dict: hash map, insertion-ordered since Python 3.7, O(1) average lookup",
            "Set: unordered unique elements, O(1) average membership check"
          ],
          "interviewQA": [
            {
              "question": "When would you choose a tuple over a list, beyond just 'tuples are immutable'?",
              "keywords": ["tuple", "hashable", "dictionary key", "immutability", "performance"],
              "modelAnswer": "Because tuples are immutable and hashable (as long as their elements are), they can be used as dictionary keys or set members, which lists cannot. Tuples also signal intent — a fixed-size, heterogeneous record like (x, y) coordinates — versus a list which implies a homogeneous, resizable collection. Tuples are also slightly more memory-efficient and faster to create than lists.",
              "tip": "Lead with the hashability/dictionary-key point — that's the concrete technical difference.",
              "difficulty": "Easy"
            }
          ]
        },
        {
          "id": "py-basic-exceptions",
          "title": "Exception Handling",
          "summary": "Python's try/except model for handling errors gracefully, using EAFP-style code.",
          "keyPoints": [
            "try/except/else/finally and catching specific exception types",
            "Custom exceptions by subclassing Exception",
            "EAFP ('easier to ask forgiveness than permission') vs LBYL ('look before you leap')",
            "Context managers (with statement) for guaranteed cleanup"
          ],
          "interviewQA": [
            {
              "question": "What is the EAFP coding style in Python and why is it preferred over LBYL?",
              "keywords": ["eafp", "lbyl", "try except", "race condition", "pythonic"],
              "modelAnswer": "EAFP means you attempt the operation directly inside a try block and handle the exception if it fails, rather than checking preconditions first (LBYL, e.g. checking 'if key in dict' before accessing it). EAFP is preferred in Python partly for style and partly for correctness: checking then acting can introduce race conditions in concurrent code, since the state could change between the check and the action. It's also often faster when the exceptional case is rare, since exceptions are relatively cheap in Python compared to repeated condition checks.",
              "tip": "Use dict key access or file existence as a concrete example.",
              "difficulty": "Medium"
            }
          ]
        }
      ]
    },
    {
      "level": "Intermediate",
      "topics": [
        {
          "id": "py-inter-oop",
          "title": "Object-Oriented Programming in Python",
          "summary": "Classes, inheritance, and Python-specific OOP mechanics like dunder methods and properties.",
          "keyPoints": [
            "class definitions, __init__, self, and instance vs class attributes",
            "Inheritance and method resolution order (MRO) with multiple inheritance",
            "Dunder methods (__str__, __eq__, __len__) for operator overloading",
            "@property for computed attributes with getter/setter semantics"
          ],
          "interviewQA": [
            {
              "question": "How does Python resolve method calls when a class has multiple inheritance, and what is MRO?",
              "keywords": ["mro", "c3 linearization", "multiple inheritance", "super", "diamond problem"],
              "modelAnswer": "Python uses the C3 linearization algorithm to compute the Method Resolution Order — a consistent, predictable order in which base classes are searched for an attribute or method. You can inspect it via ClassName.__mro__. This resolves the 'diamond problem': if two parent classes share a common ancestor, C3 ensures each class appears once and before its own parents, so calling super() in each class's __init__ cooperatively chains through the whole hierarchy correctly.",
              "tip": "Mention ClassName.__mro__ as a way to actually inspect the order.",
              "difficulty": "Hard"
            }
          ]
        },
        {
          "id": "py-inter-modules",
          "title": "Modules, Packages & Virtual Environments",
          "summary": "Organizing code into reusable modules/packages and isolating project dependencies.",
          "keyPoints": [
            "Modules (.py files) vs packages (directories with __init__.py)",
            "import mechanics, sys.path, and relative vs absolute imports",
            "Virtual environments (venv) to isolate per-project dependencies",
            "requirements.txt / pyproject.toml and pip for dependency management"
          ],
          "interviewQA": [
            {
              "question": "Why do we use virtual environments in Python projects?",
              "keywords": ["virtual environment", "dependency isolation", "venv", "version conflict", "site-packages"],
              "modelAnswer": "A virtual environment creates an isolated Python installation with its own site-packages directory, so each project can install the exact package versions it needs without conflicting with other projects or the system Python. Without it, two projects needing different versions of the same library would clash on a single global installation, and upgrading one project's dependency could silently break another. Tools like venv, virtualenv, or poetry manage this isolation and let you pin exact versions via requirements.txt or pyproject.toml.",
              "tip": "Give a concrete example: Project A needs Django 3, Project B needs Django 4.",
              "difficulty": "Easy"
            }
          ]
        },
        {
          "id": "py-inter-files-context",
          "title": "File Handling & Context Managers",
          "summary": "Reading/writing files safely and building custom context managers for resource management.",
          "keyPoints": [
            "open() modes (r, w, a, b) and always using 'with' for automatic closing",
            "Context manager protocol: __enter__ / __exit__",
            "contextlib.contextmanager decorator for a simpler generator-based approach",
            "Working with JSON/CSV via the standard library"
          ],
          "interviewQA": [
            {
              "question": "How would you write a custom context manager, and why is it better than a try/finally block?",
              "keywords": ["context manager", "__enter__", "__exit__", "with statement", "contextlib"],
              "modelAnswer": "You implement __enter__ (setup, returns the resource) and __exit__ (cleanup, receives exception info if one occurred) on a class, or use @contextlib.contextmanager to write it as a generator function with a single yield separating setup from teardown. It's not strictly 'better' than try/finally, but it's more reusable and readable — the cleanup logic is defined once and reused everywhere with 'with', rather than duplicated across every call site, and it composes cleanly with multiple resources in one with statement.",
              "tip": "Mention the @contextmanager decorator as the quick, Pythonic way to build one.",
              "difficulty": "Medium"
            }
          ]
        },
        {
          "id": "py-inter-decorators-generators",
          "title": "Decorators & Generators",
          "summary": "Two of Python's signature features: wrapping functions with decorators and lazy iteration with generators.",
          "keyPoints": [
            "Decorators as higher-order functions that wrap behavior (logging, caching, auth checks)",
            "functools.wraps to preserve metadata of the wrapped function",
            "Generators (yield) for lazy, memory-efficient iteration over large/infinite sequences",
            "Generator expressions vs list comprehensions"
          ],
          "interviewQA": [
            {
              "question": "What's the difference between a generator and a regular function that returns a list, and when does it matter?",
              "keywords": ["generator", "yield", "lazy evaluation", "memory efficiency", "iterator"],
              "modelAnswer": "A regular function computes the entire result and returns it as one object in memory; a generator function uses 'yield' to produce values one at a time, pausing its state between calls, so the full sequence is never materialized at once. This matters enormously for large or infinite sequences — processing a huge file line by line with a generator uses constant memory, while reading it all into a list could exhaust RAM. The trade-off is you can only iterate a generator once, and you lose random access (no indexing).",
              "tip": "Use processing a multi-GB file line by line as the motivating example.",
              "difficulty": "Medium"
            }
          ]
        }
      ]
    },
    {
      "level": "Advanced",
      "topics": [
        {
          "id": "py-adv-concurrency",
          "title": "Concurrency: GIL, Threading & Asyncio",
          "summary": "How Python handles parallel and concurrent work, and the constraints imposed by the Global Interpreter Lock.",
          "keyPoints": [
            "GIL allows only one thread to execute Python bytecode at a time",
            "threading module: good for I/O-bound work despite the GIL (I/O releases the GIL)",
            "multiprocessing: true parallelism for CPU-bound work via separate processes",
            "asyncio: single-threaded cooperative concurrency for high-throughput I/O-bound workloads"
          ],
          "interviewQA": [
            {
              "question": "Given the GIL, how would you speed up a CPU-bound Python task versus an I/O-bound one?",
              "keywords": ["gil", "cpu-bound", "io-bound", "multiprocessing", "asyncio", "threading"],
              "modelAnswer": "For CPU-bound work (heavy computation), threading won't help because the GIL prevents true parallel bytecode execution — instead use multiprocessing, which spawns separate processes each with its own interpreter and GIL, achieving real parallelism across CPU cores at the cost of higher memory usage and IPC overhead. For I/O-bound work (network calls, file/database access), threading works fine because the GIL is released during I/O waits, or better, use asyncio for very high-concurrency I/O since it avoids thread context-switching overhead entirely with an event loop and coroutines.",
              "tip": "Be precise: threading helps I/O-bound, multiprocessing helps CPU-bound, asyncio scales I/O-bound further.",
              "difficulty": "Hard"
            }
          ]
        },
        {
          "id": "py-adv-web-frameworks",
          "title": "Django & Flask Web Frameworks",
          "summary": "Building web applications and APIs with Python's two most common frameworks, and when to choose each.",
          "keyPoints": [
            "Django: batteries-included (ORM, admin, auth, migrations) — good for larger, structured apps",
            "Flask: minimal microframework, add only what you need — good for APIs and smaller services",
            "Django ORM QuerySets, migrations, and the MTV (Model-Template-View) pattern",
            "Flask extensions (Flask-SQLAlchemy, Flask-RESTful) for building up functionality"
          ],
          "interviewQA": [
            {
              "question": "How would you decide between Django and Flask for a new project?",
              "keywords": ["django", "flask", "batteries included", "microframework", "scalability", "flexibility"],
              "modelAnswer": "Django is a good fit when you need a full-featured app quickly — built-in admin panel, ORM, authentication, and a strong 'convention over configuration' structure that scales well across a team, especially for content-heavy or CRUD-heavy applications. Flask is better when you want fine-grained control over architecture, are building a lightweight API or microservice, or want to hand-pick libraries (e.g., a specific ORM or serializer) rather than accept Django's defaults. Many teams also use FastAPI now for high-performance async APIs with automatic OpenAPI docs, which sits between the two in philosophy.",
              "tip": "It's fine to mention FastAPI as a modern third option interviewers may probe on.",
              "difficulty": "Medium"
            }
          ]
        },
        {
          "id": "py-adv-data-science",
          "title": "Data Science with Pandas & NumPy",
          "summary": "Vectorized numerical computation and tabular data manipulation, the foundation of Python's data science stack.",
          "keyPoints": [
            "NumPy ndarrays: contiguous typed memory, vectorized operations avoid slow Python loops",
            "Pandas DataFrame/Series built on NumPy, with labeled indices and missing-data handling",
            "Vectorization vs apply() vs iterrows() performance trade-offs",
            "Broadcasting rules for operating on arrays of different shapes"
          ],
          "interviewQA": [
            {
              "question": "Why is a NumPy array operation typically much faster than an equivalent Python for-loop?",
              "keywords": ["vectorization", "numpy", "contiguous memory", "simd", "c implementation"],
              "modelAnswer": "NumPy stores array data in contiguous, fixed-type memory blocks (unlike Python lists, which store pointers to scattered objects), and its operations are implemented in compiled C code that can use vectorized CPU instructions (SIMD) to process multiple elements per instruction. A Python for-loop, by contrast, incurs interpreter overhead and dynamic type checking on every single iteration. This is why 'vectorizing' code — replacing explicit loops with NumPy/Pandas operations — is one of the biggest performance wins in data science code.",
              "tip": "Mention that Pandas' apply() is often not much faster than a loop — true vectorized ops are the real win.",
              "difficulty": "Hard"
            }
          ]
        },
        {
          "id": "py-adv-performance",
          "title": "Performance Optimization & Cython",
          "summary": "Profiling and speeding up Python code when pure-Python performance isn't enough.",
          "keyPoints": [
            "Profiling first (cProfile, line_profiler) before optimizing blindly",
            "Algorithmic improvements usually beat micro-optimizations",
            "Cython / C extensions to compile hot paths to native code",
            "Caching with functools.lru_cache for expensive repeated computations"
          ],
          "interviewQA": [
            {
              "question": "A Python function is running too slowly in production. Walk through your approach to fixing it.",
              "keywords": ["profiling", "cprofile", "bottleneck", "algorithmic complexity", "cython", "caching"],
              "modelAnswer": "First, profile before guessing — use cProfile or py-spy to find the actual bottleneck rather than assuming. Often the fix is algorithmic: replacing an O(n^2) approach with a hash-based O(n) one, or eliminating redundant recomputation with functools.lru_cache. If the bottleneck is genuinely CPU-bound Python overhead in a tight loop, consider vectorizing with NumPy, rewriting the hot path in Cython or using a library like Numba for JIT compilation, or as a last resort, moving that piece into a compiled extension or a different service.",
              "tip": "Always start the answer with 'profile first' — that's what interviewers want to hear.",
              "difficulty": "Medium"
            }
          ]
        }
      ]
    }
  ]
}
